/**
 * it will have a default constructor without parameter
 *
 * blic, private ,protected, internal
 * final, abstract, open
 */
class Person {
    // define two property
    var name: String = ""
    var age: Int = 0

    // define a method
    fun say(content: String) {
        println(content)
    }
}